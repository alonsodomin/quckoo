kairos {
  home-dir = "."
  home-dir = ${?CHRONOS_HOME}

  resolver {
    cacheDir = ${chronos.home-dir}"/cache"
    workDir = ${chronos.home-dir}"/work"

    repositories = [ ]

  }

  task-queue {
    max-work-timeout = 10m
  }
}